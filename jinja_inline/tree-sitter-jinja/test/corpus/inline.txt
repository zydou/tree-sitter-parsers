==============
if
==============

# for href, caption in [('index.html', 'Index'),
                        ('about.html', 'About')]:

# if loop.last
# endif
    {{ statement }}

---

(source
  (inline)
  (inline)
  (inline)
  (words)
  (render_expression
    (expression
      (binary_expression
        (unary_expression
          (primary_expression
            (identifier))))))
  (words))
